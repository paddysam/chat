# 复制本文件为 .env.local 后修改值。.env.local 已被 gitignore，不会推到仓库。
#
# 开发环境下，Vite dev server 会把 /api/* 反向代理到下面这个上游：
#   /api/v1/chat/completions  →  ${VITE_API_TARGET}/v1/chat/completions
# 改完后需重启 `npm run dev`。
VITE_API_TARGET=https://api.openai.com
